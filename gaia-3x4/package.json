{
  "name": "gaia-3x4",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GA.IA/3x4 — uma linguagem de relações situadas e recortes responsáveis",
  "engines": {
    "node": ">=22.15"
  },
  "scripts": {
    "start": "npm run build && node scripts/serve.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "demo": "node scripts/demo.mjs",
    "verify:browser": "npm run build && node scripts/verify-browser.mjs",
    "verify:performance": "npm run build && node scripts/verify-performance.mjs",
    "verify:labour": "npm run build && node scripts/verify-labour.mjs",
    "verify:data-born": "npm run build && node scripts/verify-data-born.mjs",
    "capture:power": "node scripts/capture-nasa-power.mjs"
  },
  "dependencies": {
    "@codemirror/autocomplete": "^6.20.3",
    "@codemirror/commands": "^6.11.0",
    "@codemirror/language": "^6.12.4",
    "@codemirror/lint": "^6.9.7",
    "@codemirror/search": "^6.7.2",
    "@codemirror/state": "^6.7.4",
    "@codemirror/view": "^6.43.11",
    "@lezer/highlight": "^1.2.3",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
